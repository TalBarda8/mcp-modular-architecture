# =============================================================================
# MCP Modular Architecture - Environment Variables Example
# =============================================================================
#
# This file documents all environment variables supported by the application.
# Copy this file to `.env` and customize values for your local environment.
#
# IMPORTANT: This project does NOT require any secrets or API keys to run.
#            All variables are optional with sensible defaults.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Controls which configuration file is loaded from the config/ directory
#
# Supported values:
#   - development: Loads config/development.yaml (default)
#   - production:  Loads config/production.yaml
#   - server:      Loads config/server.yaml (used by run_server.py)
#   - test:        For testing environments
#
# Default behavior if not set: 'development'
# Used by: ConfigManager (src/core/config/config_manager.py)
#
APP_ENV=development

# -----------------------------------------------------------------------------
# Future Placeholders (Optional - Not Currently Used)
# -----------------------------------------------------------------------------
# The following variables are placeholders for future functionality.
# They are NOT required for current operation.

# API Keys (if external services are added in the future)
# Example: OPENAI_API_KEY=your-api-key-here
# Example: ANTHROPIC_API_KEY=your-api-key-here

# Database Configuration (if database support is added)
# Example: DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Logging Level Override (currently managed via YAML config)
# Example: LOG_LEVEL=DEBUG

# Custom Configuration Path (if non-standard location needed)
# Example: CONFIG_PATH=/path/to/custom/config

# =============================================================================
# Usage Instructions
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your local values (optional - defaults work fine)
#
# 3. The .env file is gitignored and will NOT be committed
#
# 4. For production deployment, set APP_ENV=production
#
# 5. For running the MCP server, APP_ENV=server is set automatically
#    by run_server.py
#
# =============================================================================
# Security Notes
# =============================================================================
#
# - Never commit .env files to version control
# - This project currently requires NO secrets to run
# - If you add API keys in the future, ensure .env is in .gitignore
# - Use config/local.yaml for local config overrides (also gitignored)
#
# =============================================================================
